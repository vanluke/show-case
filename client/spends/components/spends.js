import React, { Component } from 'react';

export default class Spends extends Component {
  constructor(props) {
    super(...props);
  }

  componentDidMount() {
  }

  render() {
    return (<div>
      This is your spends
    </div>);
  }
}
